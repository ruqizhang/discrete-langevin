A toy example for categorical sampling. The default parameters sample from a distribution with 2 dimensions and 3 classes for each dimension.

Run 
```
python sample_categorical_distribution.py --sampler dmala
```
